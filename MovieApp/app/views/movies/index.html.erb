
<div class="main">
	<%= @movies.each do |m| %>
		<div class="movie">
		<%= image_tag m.image %>
		<h3> <%= m.title %> </h3>
		<p> <%= m.release_year %> </p>
		<p> <%= m.plot %> </p>
		<%= link_to "Learn more", movie_path(m) %>
		</div>
	<% end %>
</div>
